# === Required ===
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-proj-...
REDIS_URL=redis://localhost:6379/1
SECRET_KEY_BASE=  # generate: ruby -rsecurerandom -e 'puts SecureRandom.hex(64)'

# === Optional (sensible defaults) ===
# CLAUDE_MODEL=claude-sonnet-4-5-20250514
# STT_API_KEY=              # falls back to OPENAI_API_KEY
# STT_PROVIDER=whisper_api  # whisper_api | whisper_local | deepgram
# STT_ENDPOINT=             # only for whisper_local
# AUDIO_CHUNK_SECONDS=3
# RAILS_MAX_THREADS=5
# RAILS_LOG_LEVEL=info
# FORCE_SSL=                # set to "true" if PaaS doesn't terminate TLS
